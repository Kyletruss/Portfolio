import { extendTheme, ThemeConfig } from "@chakra-ui/react";

// Ensure your theme config is correct
const config = {
  initialColorMode: "light", // or "dark"
  useSystemColorMode: false,
};

const theme = { config };

export default theme;
